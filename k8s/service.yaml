apiVersion: v1
kind: Service
metadata:
  name: node-app-service
  labels:
    app: node-app
spec:
  type: LoadBalancer # Can be NodePort or LoadBalancer depending on the cluster
  selector:
    app: node-app
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
